name: build
run-name: docker-ci
on: [push]
jobs:
  build_projects:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3
        - name: setup java
          uses: actions/setup-java@v3
          with:
            distribution: 'temurin'
            java-version: '17'
            cache: 'maven'
        - name: Build with Maven
          run: |  
            mvn install -f ./micro-authentication/pom.xml
            mvn install -f ./micro-gateway/pom.xml
            mvn install -f ./micro-restaurant/pom.xml